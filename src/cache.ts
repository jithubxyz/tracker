import { Tracker, Client, Token } from './types';

export const trackers = new Map<string, Tracker>();
export const clients = new Map<string, Client>();
export const tokens = new Map<string, Token>();
